pre-commit:
  commands:
    typecheck:
      glob: "*.{js,ts,jsx,tsx}"
      run: bun run tc
      exclude:
        - "*.test.*"
        - "*.spec.*"

pre-push:
  commands:
    typecheck:
      run: bun run tc
    build:
      run: bun run build
